package com.example.facturation.support.paiement.domain.model;

/**
 * Supporting Domain (Paiement) — Enumération des modes de paiement.
 */
public enum ModePaiement {
    VIREMENT,
    CARTE,
    CHEQUE,
    PRELEVEMENT
}
